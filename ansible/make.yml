---
- name: tasks
  hosts: all
  vars:
    target_env: "{{ lookup('env', 'target') }}"
  tasks:
  - include_role:
      name: "{{ name }}"
    loop:
    - app-agent-1.0.0
    - appctl-1.0.1
    - arping-1.0.0
    - caddy-1.0.3
    - jq-1.0.1
    - harbor
    loop_control:
      loop_var: name
