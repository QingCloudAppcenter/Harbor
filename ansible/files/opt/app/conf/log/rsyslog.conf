#  Rsyslog configuration file for docker.

template(name="DynaFile" type="string"
	string="/var/log/docker/%syslogtag:R,ERE,0,DFLT:[a-z]*--end:secpath-replace%.log"
)
if $programname != "rsyslogd" then {
  :msg, contains, "redis://anonymous" ~
  -?DynaFile
}
